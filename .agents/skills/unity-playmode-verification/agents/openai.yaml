interface:
  display_name: "Unity Play Mode Verification"
  short_description: "Focused Play Mode verification for runtime behavior."
  default_prompt: "Use $unity-playmode-verification to verify only the relevant runtime behavior."
