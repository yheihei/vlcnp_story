interface:
  display_name: "Unity Play Mode Verification"
  short_description: "変更した挙動に絞ってUnityのPlay Modeで観察・検証する"
  default_prompt: "Use $unity-playmode-verification to observe the runtime behavior relevant to the requested change."
